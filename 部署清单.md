# 📋 JK BMS WebHook 版部署清单

> 版本：固件 v2.17（4MB Flash 双 OTA）/ 服务端 v2.17（WebHook 架构，无常驻进程）
> 适用范围：虚拟主机 / 宝塔部署

## 一、硬件清单与接线

| 硬件 | 说明 |
|---|---|
| ESP32-S3 DevKit（建议带 PSRAM） | 主控，BLE 读取 BMS |
| 极空（JK）智能保护板 | 支持 BLE，服务 UUID `ffe0` / 特征 `ffe1`（新版双 FFE1，固件已适配） |
| 银尔达 M100PG-DTU（4G + GPS 一体） | MQTT 透传 + GPS 定位 |
| SIM 卡（物联网流量卡） | 4G 联网 |
| 电源 | 12V 或电池供电，经**隔离降压模块（输出 5V，≥2A）**为 DTU + ESP32 供电 |
| 连接线 | ESP32 UART ↔ DTU 串口，交叉接线 |

**接线（ESP32 UART1 与 DTU 串口对接）：**

| ESP32-S3 | 银尔达 M100PG-DTU | 说明 |
|---|---|---|
| GPIO 12（UART1 RX） | TX | 接收 DTU 数据（下行指令 + GPS） |
| GPIO 13（UART1 TX） | RX | 发送数据给 DTU（上行转发） |
| GND | GND | 共地 |
| 5V | VIN | 供电（隔离降压模块输出 **5V，≥2A**；4G 峰值电流大，建议 VIN 与 GND 间并联 16V 1000μF 电解电容防掉压） |

> 注：本方案 GPS 由 DTU 自带（ESP32 经串口指令 `config,get,gpsext` 查询），**不接保护板的 GPS 供电口**。

**固件与上报规则：**
> 固件：`02-ESP32固件/esp32s3-gateway.ino`（v2.17，**ESP32-S3 Super Mini 4MB Flash**，Arduino IDE，需 Arduino-ESP32 Core ≥ 3.0.0 + NimBLE-Arduino ≥ 2.2.2 + WebSockets + ArduinoJson）。编译要点：Board=ESP32S3 Dev Module、Partition Scheme=**Custom**（同目录 `partitions.csv`：4MB 双 OTA 分区）、Flash Size=**4MB**、PSRAM=OPI。
> **首次烧录**：`02-ESP32固件/firmware/esp32s3-gateway-v2.17-4MB-OTA.merged.bin`（仓库已发布，含 4MB 分区表）用 Flash Download Tool / esptool 全量刷。**之后升级**：连 ESP32 Web 后台（192.168.4.1）→ 固件升级 → 上传 `esp32s3-gateway-v2.17-OTA.bin`（OTA，成功自动重启、失败自动回滚），无需拆机/串口。
> **上报规则**：GPS 移动 2s 实时 / 静止 ≥60s 停发（网页开/关一致；速度≥3km/h 且位移≥30m 双条件判运动防漂移）；BMS 网页打开 2s 实时 / 网页关闭 30s。
> **自愈机制**：ESP32 卡死 30s 看门狗重启；DTU 串口无响应 >8 分钟重启；MQTT 下行静默断 >12 分钟重启；另有每 24 小时定时重启（防 BLE 长连接漂移）。重启后 ESP32 Web 后台「系统状态 → 上次重启」可查原因（定时重启/看门狗卡死/上电/崩溃）。可选 `DTU_POWER_PIN` 引脚冷重启 DTU（默认 -1 不启用）。

## 二、上传文件

把 `01-网页服务端\` 里的全部文件上传到虚拟主机网站根目录：

```
index.php      ← 网页看板
api.php        ← 接口（ingest/latest/trips/replay/settings/range/reboot 等）
ingest.php     ← WebHook 接收 + 解析入库 + 在线检测 + 模式下发
db.php         ← SQLite 数据库 + 完整性自愈 + 自动清理
parser.php     ← 文本/BMS 帧解析
range.php      ← 里程计算 + 自动学习
login.php / logout.php / auth.php   ← 登录鉴权
config.php     ← 配置（需要改，见下）
.htaccess      ← 目录保护（Apache 生效）
data/          ← 空目录（建库/缓存/日志用，需可写）
```

## 三、改 config.php

```php
// ① WebHook 令牌（EMQX 转发鉴权，改随机长串）+ MQTT 直发凭据（下行指令用）
'mqtt' => [
    'token'   => '改成你的随机长串',          // EMQX 规则转发鉴权
    'host'    => '你的EMQX地址.emqxsl.cn',   // 与 EMQX 连接地址相同
    'port'    => 8883,                       // MQTT over TLS 端口（Serverless 用 8883）
    'username'=> '你的MQTT账号',             // “访问控制”→“客户端认证”账号
    'password'=> '你的MQTT密码',             // “访问控制”→“客户端认证”密码
],

// ② 登录密码（默认 admin888 必须改）
'auth' => ['username' => 'admin', 'password' => '你的密码'],
```

> ⚠ 仓库中的 config.php 已脱敏为占位符，按上面填好后上传；**勿把真实账号密码提交到公开仓库**（本地可用 `git update-index --skip-worktree "01-网页服务端/config.php"` 防止误提交）。

## 四、确认虚拟主机环境

| 项 | 要求 |
|---|---|
| PHP | ≥ 7.4 |
| 扩展 | pdo_sqlite / sqlite3 / openssl / curl（缺啥在面板开） |
| data/ 目录 | 可写（上传后设权限 755 或面板设置） |
| 时区 | 代码已强制北京时间，无需配置 |

## 五、EMQX 控制台（2 步）

**① 客户端认证**（下行 MQTT 直发用）：部署 → 访问控制 → 客户端认证 → 添加账号密码（填进 config.php 的 mqtt 段）

**② 转发规则**：
- 数据集成 → 规则 → 创建
- SQL：`SELECT payload FROM "T"`
- 动作：**HTTP 服务** 连接器
  - URL：`http://你的域名/api.php?action=ingest`
  - 方法：POST
  - 请求头：**`X-Ingest-Token: <config.php 的 token>`**（必须加！）

## 六、验证（按顺序）

1. 浏览器打开 `http://你的域名` → 登录 → 看板正常显示
2. 手机流量访问（确认公网可访问）
3. 设备/DTU 上报 → 页面出现数据
4. `data/ingest.log` 出现 GPS/BMS/blestatus 记录 = 全链路通
5. 打开网页 → 日志出现 `mode 下发成功(MQTT直发)`，数据变 2s 实时；关网页 → 变 30s
6. 里程学习：设置页「里程设置」显示 `💤 等待首次充电`；充一次电后变 `✅ 基准就绪`；骑行后再次充电出现学习记录

## 七、可选：cron 兜底

虚拟主机控制面板 → 定时任务，每分钟执行：

```
wget -q -O- "http://你的域名/api.php?action=checkmode&token=<token>"
```

作用：设备 30s 才上报一次时，及时收敛网页关闭后的离线状态（服务端已内置 30s 周期强制下行，此项仅兜底加速）。

## 部署后自检清单

- [ ] 全部文件 + .htaccess + data/ 已上传
- [ ] config.php 已改（token / MQTT 直发凭据 / 登录密码）
- [ ] EMQX 客户端认证已建（下行直发）
- [ ] EMQX 转发规则已建，X-Ingest-Token 头已加
- [ ] 网页打开显示数据，关闭后日志降为 30s
- [ ] 手机流量能访问
- [ ] ingest.log 有记录
- [ ] （可选）cron 已配

## 安全加固（内置，建议逐项确认）

| 项 | 说明 |
|---|---|
| 内部文件防直接访问 | 所有内部 php（config/db/parser/range/ingest/auth）顶部守卫：必须由入口文件加载，直接访问一律 403。**Nginx（宝塔默认）不读 .htaccess，此守卫是主要防线**；Apache 下 .htaccess 双保险 |
| **data/ 目录保护（重要）** | `data/` 内含全量轨迹 `bms.db`、实时定位 `latest.json`、`ingest.log`。Apache 由 `.htaccess` 拦截；**Nginx/宝塔必须在站点配置加**：`location ~ ^/data/ { deny all; }`。`data/index.php` 守卫兜底 |
| CSRF 防护 | api.php 的 settings/range/reboot POST 校验 Origin/Referer 同源 |
| 登录失败限速 | 5 分钟失败 5 次锁定 5 分钟（按客户端 IP 计数，存 SQLite，登录成功清零） |
| 密码哈希存储 | bcrypt 哈希入库（旧明文自动兼容），新密码最短 6 位 |
| 会话 Cookie | HttpOnly + SameSite=Lax；HTTPS 下自动加 Secure |
| ingest 大小上限 | WebHook 接收体限制 64KB（双重校验），防 DoS |
| 心跳表上限 | clients.json 最多保留 50 条，防 uid 刷爆 |
| ⚠ 反代/CDN 提示 | 登录限速按 `REMOTE_ADDR` 计数：套 CDN/多层反代时所有用户共享同一 IP，攻击者 5 次失败即可锁死全站 5 分钟。建议反代场景下调大 `LOGIN_MAX_FAILS` |
| ⚠ HTTPS/Secure 提示 | 宝塔 Nginx + PHP-FPM 可能不传 `HTTPS`，cookie Secure 不生效。开 SSL 后确认站点配置含 `fastcgi_param HTTPS $https if_not_empty;` |

**仍建议手动做（无法代码兜底）：**
1. 登录密码改掉默认 `admin888`（设置页 → 登录账号设置）
2. 开启 HTTPS（虚拟主机面板一键 SSL），避免密码明文传输
3. 高德开放平台把 JS API Key 绑定你的域名白名单（Key 在前端必然可见，绑定域名可防盗用）
4. webhook token 换成长随机串（config.php）

## 常见问题排查

| 现象 | 处理 |
|---|---|
| 网页打不开 / 白屏 | 检查 php 版本与扩展（pdo_sqlite/curl/openssl）；`php -l` 各文件无语法错误 |
| EMQX 动作失败 401/403 | X-Ingest-Token 值与 config.php 不一致；确认请求头已加 |
| 数据 30 秒才更新 | 网页没开（BMS 30s / GPS 静止停发）；打开网页即 2s 实时 |
| 网页时间差 8 小时 | 上传最新 db.php（已强制北京时间） |
| 数据很久不动（GPS） | 车辆静止 ≥60s 后 GPS 自动停发属正常；移动后自动恢复 2s；若静止仍频繁上报说明漂移误判，调大固件 `G_GPS_MOVE_DIST_M` |
| 里程学习不结算 | 需完成一次充电周期：充电结束(记录基准) → 骑行 → 再次充电(结算)；确认充电/放电时 BMS 数据在上报；仍不行看 ingest.log 有无 `[里程学习]` 记录 |
| 远程重启无效 | 下行链路不通（网页看不到"实时模式"）；先开一次网页触发下行，或由自愈机制兜底 |
