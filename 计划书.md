# JK BMS 4G 远程监控系统 · 设计文档

> 版本：固件 v2.15（4MB Flash 双 OTA，BLE 基于中继完整版 v9.43）/ 服务端 v2.15（WebHook 架构，无常驻进程）
> 架构来源：《JK-BMS-4G-main》（Node.js 服务端方案）与《极空保护板-蓝牙4G方案》（浏览器直连 MQTT + 高德地图方案）的简化整合。

## 一、项目目标

用最简单可靠的架构实现锂电池（极空/JK 保护板）BMS 远程监控：

1. **ESP32-S3** 通过 BLE 读取极空保护板实时数据；
2. 经**银尔达 M100PG-DTU（4G + GPS 一体）**通过 MQTT 上传；
3. **PHP 服务端**（WebHook 架构，纯虚拟主机可部署，无常驻进程）接收、解析、存储、展示；
4. 网页打开时**实时刷新**（GPS/BMS 2 秒级），展示 BMS 实时数据、GPS 实时定位、速度、续航里程；
5. 网页关闭后设备自动降频：GPS 移动时 2s 记录轨迹（静止停发）、BMS 30s 上报，保证轨迹回放与里程/续航学习不断档，同时大幅省流量。

## 二、总体架构

```
┌────────────┐   BLE   ┌────────────────────┐  UART  ┌───────────────────┐  MQTT  ┌────────────────┐  HTTP轮询  ┌──────────┐
│  极空 BMS   │◀──────▶│   ESP32-S3 固件      │◀─────▶│ 银尔达 M100PG-DTU  │◀─────▶│  EMQX Serverless│◀─────────▶│  网页浏览器 │
│ (BLE外设)   │        │  · BLE 读取 BMS      │ 透传  │  · 4G 联网         │  T/R  │  · 规则引擎转发  │  /api/*   │ 实时看板   │
└────────────┘        │  · BLE 中继(App接入) │       │  · GPS 定位        │  主题  │  · MQTT 直发下行 │           └──────────┘
                      │  · Web 后台(AP模式)  │       │  · MQTT 双向透传    │        │        │
                      │  · GPS 解析/模式控制  │       └───────────────────┘        └────────┬───────┘
                      └────────────────────┘                                             │ WebHook POST
                          ▲                                                              ▼
                          │ 手机浏览器/极空App (192.168.4.1)                       ┌────────────────┐
                                                                                 │ PHP 服务端(虚拟主机) │
                                                                                 │ · api.php?action=ingest│
                                                                                 │ · SQLite / 缓存 / 里程 │
                                                                                 └────────────────┘
```

**数据流：**
- 上行：BMS 原始帧 / GPS 数据 → ESP32 组帧为文本 → UART → M100PG-DTU → MQTT `T` 主题 → **EMQX 规则引擎自动转发**（HTTP POST → `api.php?action=ingest`，X-Ingest-Token 鉴权）→ SQLite + 缓存 → 网页轮询显示；
- 下行：网页打开/关闭状态（heartbeat）→ api.php 用 EMQX「客户端认证」账号 **MQTT 直连发布**（TLS 8883）到 `R` 主题 → M100PG-DTU → UART → ESP32 切换上报频率；
- 服务端**无常驻进程**，纯网页 PHP 即可承载（虚拟主机/宝塔直接部署）。

## 三、硬件清单与接线

| 硬件 | 说明 |
|---|---|
| ESP32-S3 DevKit（建议带 PSRAM） | 主控，BLE 读取 BMS |
| 极空（JK）智能保护板 | 支持 BLE，服务 UUID `ffe0` / 特征 `ffe1`（新版双 FFE1，固件已适配） |
| 银尔达 M100PG-DTU（4G + GPS 一体） | MQTT 透传 + GPS 定位 |
| SIM 卡（物联网流量卡） | 4G 联网 |
| 电源 | 12V 或电池供电，经**隔离降压模块（输出 5V，≥2A）**为 DTU + ESP32 供电 |
| 连接线 | ESP32 UART ↔ DTU 串口，交叉接线 |

**接线（ESP32 UART1 与 DTU 串口对接）：**

| ESP32-S3 | 银尔达 M100PG-DTU | 说明 |
|---|---|---|
| GPIO 12（UART1 RX） | TX | 接收 DTU 数据（下行指令 + GPS） |
| GPIO 13（UART1 TX） | RX | 发送数据给 DTU（上行转发） |
| GND | GND | 共地 |
| 5V | VIN | 供电（隔离降压模块输出 **5V，≥2A**；4G 峰值电流大，建议 VIN 与 GND 间并联 16V 1000μF 电解电容防掉压） |

> 注：本方案 GPS 由 DTU 自带（ESP32 经串口指令 `config,get,gpsext` 查询），**不接保护板的 GPS 供电口**；参考方案中「保护板 GPS 口正负极、接反烧模块」的警示仅适用于该方案，此处不涉及。

## 四、ESP32-S3 固件设计

开发框架：Arduino（NimBLE 库）。单文件 `esp32s3-gateway.ino`（v2.15，约 2500 行），适配 **ESP32-S3 Super Mini（4MB Flash）**。**BLE 部分合入中继完整版 v9.43 全部修复**（FIX-22~27 + v9.13~v9.43：断连重建防崩溃、回调安全、AT 心跳保活、连接参数抗挤占、扫描退避、MTU 降级、隐身广播、命令缓存补发、手动扫描）。

**分区与 OTA**：`partitions.csv`（与 .ino 同目录）自定义 4MB 分区 —— 双 APP 分区（app0/app1 各 1.94MB），支持 **Web 后台网页 OTA**（上传 .bin 写另一分区，成功切换重启、失败自动回滚）。Arduino IDE 编译时 Partition Scheme 选 **Custom**、Flash Size 选 **4MB**。

### 4.1 功能模块

| 模块 | 说明 |
|---|---|
| BLE 客户端 | 扫描指定 MAC 的极空 BMS，连接 `ffe0` 服务，**必须用 NimBLE-Arduino 库**；按 handle 区分双 FFE1 特征（0x03 写命令 / 0x05 收通知），适配 V20S 双 FFE1 与 V17/V18/V19 单 FFE1 等布局（特征选择"属性驱动 + handle 优先"，订阅按能力选 notify/indicate） |
| 新旧极空兼容 | 帧头双兼容：老版 `55 AA EB 90`（300B + CRC@299）；新版 `AA 55 90 EB`（~200B 无 CRC，>200 字节即收帧） |
| 拼帧器 | 兼容两种帧头拼帧，CRC 校验（老版），缓存最新帧 |
| 帧级转发 | BMS 数据重组完整帧 + CRC 校验通过后才转发，坏帧自愈对齐 |
| BLE 中继 | 多客户端（最多 5 个）按各自 MTU 分块推送（150B 块模拟真实 BMS）；AT 心跳 / 命令帧回显原样透传；广播名使用中继名称（Web 后台可改、默认 `JK-RELAY`，UTF-8 截断 ≤20 字符） |
| GPS 解析 | 通过 DTU 指令查询：`config,get,gpsext` → 解析 `config,gps,ok,<fix>,<lonDir>,<lon>,<latDir>,<lat>,<knots>` → 节转 km/h 组帧 `gps:<fix>,<lonDir>,<lon>,<latDir>,<lat>,<speed>` |
| 压缩编码 | 将原始帧压缩为 `b:r,<hex>` 文本（连续 2+ 个 `0x00` 用 `!n!` 表示），经 UART1 发出 |
| 串口发送保护 | `sendSerial1()` 非阻塞排队（≥250ms 间隔），防多条数据粘连；禁止阻塞 `delay()`（会触发看门狗崩溃） |

### 4.2 上报规则（核心）

| 数据 | 规则 | 说明 |
|---|---|---|
| **GPS** | 移动 **2s 实时** / 静止 ≥60s **停发** | 网页开/关一致；静止/运动用**双条件**判断：速度 ≥3km/h **且** 位移 ≥30m（防 GPS 漂移误判，阈值常量 `G_GPS_MOVING_KMH`/`G_GPS_MOVE_DIST_M` 可调）；静止时每 30s 本地探测一次，移动即恢复 |
| **BMS** | 网页打开 **2s** / 网页关闭 **30s** | 充/放电/停放一致；网页开=实时查看，网页关=保里程与续航学习（30s 采样 Ah 积分误差极小） |
| 蓝牙状态 | 每 30s 主动上报 | `blestatus:<state>,<mac>,<connected>`，网页蓝牙状态实时反映真机，同时作设备保活信号 |

### 4.3 下行控制

- 服务端下发 `mode,realtime,<bms_ms>,<gps_ms>` / `mode,track,<bms_ms>,<gps_ms>` 指令（经 MQTT `R` 主题 → DTU → UART）；
- 网页打开 → realtime（2s 级）；网页关闭 → track（30s）；
- 服务端每 30s 周期强制重发当前 mode 指令（下行保活探测）。

### 4.4 自愈机制（免人工断电）

| 层 | 机制 | 参数 |
|---|---|---|
| 任务看门狗 | ESP32 卡死自动重启 | 30s |
| DTU 串口无响应 | 收不到任何 DTU 串口数据 → 重启 | >8 分钟 |
| MQTT 下行静默断 | 收不到 EMQX 下行指令（mode/chaxun 等）→ 重启（覆盖 DTU 长连接静默断开典型故障） | >12 分钟 |
| DTU 冷重启（可选） | `DTU_POWER_PIN` 引脚断电重启 DTU（需外接 MOS/继电器） | 默认 -1 不启用 |
| **重启原因记录** | 每次重启记录原因（定时重启/看门狗卡死/上电/崩溃），Web 后台「系统状态」显示 | v2.14 |
| **OTA 网页升级** | Web 后台「固件升级」上传 .bin，双 OTA 分区写入另一分区，成功切换/失败回滚 | v2.14 |
| **BLE 稳定性** | 合入中继完整版 v9.43：断连重建防崩溃/回调安全/AT心跳保活/MTU降级/隐身广播/命令缓存补发/手动扫描 | v2.15 |

## 五、4G/GPS 串口数据格式（UART1）

| 方向 | 指令/数据 | 说明 |
|---|---|---|
| ESP32→DTU | `config,get,csq` | 查信号强度，DTU 回 `config,csq,ok,<rssi>,<csq>,...`，固件组帧 `csq:<csq>` |
| ESP32→DTU | `config,get,gpsext` | 查 GPS，DTU 回 `config,gps,ok,<fix>,<lonDir>,<lon>,<latDir>,<lat>,<knots>,...` |
| ESP32→DTU | `chaxun` | 手动触发查询（保留） |
| DTU→ESP32 | `mode,...` / `getblestatus` / `getcsq` / `getgps` | EMQX 下行指令经 DTU 透传 |
| ESP32→服务端 | `b:r,<hex>` / `gps:...` / `csq:...` / `blestatus:...` | 组帧上行，服务端 parser.php 解析 |

> 顺序查询避免粘连：本周期先发 csq，DTU 回 `config,csq,ok` 后再发 gpsext，同一周期内 csq 与 GPS 同步刷新；csq 超时未响应则下轮直接查 gpsext 兜底。

## 六、PHP 服务端设计（WebHook 架构）

### 6.1 文件结构

```
01-网页服务端/
├── index.php      # 网页看板（单文件：GPS/BMS/轨迹/设置/登录）
├── api.php        # 接口路由（ingest/latest/heartbeat/track/trips/replay/dailyStats/settings/range/reboot...）
├── ingest.php     # WebHook 接收：文本解析入库 + 在线检测 + 双模式下发 + 里程学习
├── parser.php     # BMS 帧解析（移植自 bms-parser.js）+ GPS/文本解析
├── range.php      # 里程计算 + 自动学习 + WGS84→GCJ02 转换
├── db.php         # SQLite 连接 + 完整性自愈 + 自动清理
├── login.php / logout.php / auth.php   # 登录鉴权（bcrypt + 限速）
├── config.php     # 配置（MQTT 凭据 / token / 登录密码 / 上报间隔）
├── .htaccess      # 目录保护（Apache）
└── data/          # bms.db / latest.json / clients.json / ingest.log
```

### 6.2 关键设计

- **WebHook 接收**：EMQX 规则把 `T` 主题消息 POST 到 `api.php?action=ingest`（X-Ingest-Token 鉴权）→ 解析 `b:r`（BMS 帧）/ `gps` / `csq` / `blestatus` → 入库 + 缓存；
- **下行 MQTT 直连**：api.php 用 EMQX「客户端认证」账号 TLS 直连发布到 `R` 主题（纯 PHP socket 实现 MQTT 3.1.1，零依赖）；
- **在线检测与模式切换**：网页 heartbeat（5s）维护客户端表；在线数 0→1 发 realtime，1→0 发 track；状态持久化到 `app_settings`（HTTP 无状态，跨请求共享）；设置变化立即重下发当前模式；
- **数据库自愈**：`db()` 打开时 `PRAGMA integrity_check`，损坏自动备份（`.corrupted.时间戳`）并重建；写库 try-catch；
- **数据量控制**：`dataRetentionDays`（默认 60 天，网页可调）自动清理过期数据 + `VACUUM` 回收空间；`ingest.log` 超 1MB 轮转保留 2 份；心跳表上限 50 条；
- **时区**：代码强制北京时间（虚拟主机 UTC 环境修正）。

### 6.3 主要 API

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api.php?action=ingest` | WebHook 接收（X-Ingest-Token 鉴权） |
| GET | `/api.php?action=latest` | 最新 BMS + GPS + 蓝牙状态 + 续航（前端主轮询） |
| GET | `/api.php?action=heartbeat&uid=` | 客户端心跳（在线检测） |
| GET | `/api.php?action=track&from=&to=` | 轨迹点列表（含总里程、GCJ02 坐标） |
| GET | `/api.php?action=trips&date=` | 按天行程列表（相邻点 >5min 切分新行程） |
| GET | `/api.php?action=replay&date=&from=&to=` | 按日期/时间范围轨迹回放 |
| GET | `/api.php?action=trackdates` | 有轨迹的所有日期 |
| GET/POST | `/api.php?action=range` | 里程基数/学习开关 + 学习状态与历史 |
| GET/POST | `/api.php?action=settings` | 网页刷新间隔/BMS/GPS 间隔/保留天数 |
| POST | `/api.php?action=reboot` | 远程重启设备（下行链路通时生效） |
| GET | `/api.php?action=checkmode` | cron 兜底端点（可选） |

### 6.4 安全加固

| 项 | 说明 |
|---|---|
| 内部文件防直接访问 | 所有内部 php（config/db/parser/range/ingest/auth）顶部守卫：必须由入口文件加载，直接访问一律 403（Nginx/宝塔下是主要防线） |
| data/ 目录保护 | Apache 由 `.htaccess` 拦截；Nginx 需站点配置 `location ~ ^/data/ { deny all; }`；`data/index.php` 守卫兜底 |
| CSRF 防护 | settings/range/reboot 等 POST 校验 Origin/Referer 同源 |
| 登录限速 | 5 分钟失败 5 次锁定 5 分钟（按客户端 IP 计数，存 SQLite） |
| 密码哈希 | bcrypt 哈希存储（兼容旧明文），新密码最短 6 位 |
| 会话 Cookie | HttpOnly + SameSite=Lax，HTTPS 下自动加 Secure |
| ingest 上限 | WebHook 接收体限制 64KB（双重校验） |

## 七、数据库设计（SQLite）

> SQLite 是嵌入式文件数据库，整个库就是单个文件（`data/bms.db`），PHP 内置 `pdo_sqlite` 零配置。本项目每天数据量几千条，SQLite 完全胜任，单文件便于备份迁移。

| 表 | 关键字段 | 用途 |
|---|---|---|
| `bms_data` | ts, totalVoltage, current, power, soc, capRemain, capNominal, temp1, temp2, mosTemp, cellCount, cellVoltages(JSON), cellResist(JSON), chargeMOS, dischargeMOS, balancerOn | BMS 数据 |
| `gps_data` | ts, lon, lat, speed, fix, mode | GPS 轨迹点（实时+追踪都记录） |
| `app_settings` | key, value | 里程基数、学习开关、学习状态、当前模式、设置等 |
| `range_learning` | ts, startTs, endTs, distance, capUsed, learnedFactor, oldFactor, newFactor | 里程学习历史记录 |

索引：`bms_data(ts)`、`gps_data(ts)`。

## 八、里程与续航计算

### 8.1 实时续航估算

```
续航里程(km) = 剩余容量 capRemain(Ah) × 里程基数 rangeFactor(km/Ah)
```

- `rangeFactor` 默认 **2.0 km/Ah**，网页「里程设置」可手动调整；
- 行驶中显示 `续航 ≈ capRemain × rangeFactor`。

### 8.2 里程自动学习（可开关，随充随学）

原理（任意充电周期都学，**不要求满充**）：
1. **充电结束**：电流从 >0.1A 变 ≤0.1A → 记录当时剩余容量作**基准**（充满或中途拔掉都行，充了多少记多少）；
2. **下次充电开始**：电流从 ≤0.1A 变 >0.1A → 结算：`消耗 capUsed = 基准 − 当前剩余容量`（**>0.5Ah 才有效**）；
3. 用该周期 GPS 轨迹点（haversine 累加）得行驶距离 `distance`（**≥0.2km 才有效**）；
4. `learnedFactor = distance / capUsed`；平滑更新 `newFactor = 0.7×old + 0.3×learned`，写入 `app_settings` 并记录到 `range_learning` 表；
5. 学习状态（等待首次充电 / 基准就绪 / 充电中）与最近学习历史在网页「里程设置」实时显示，每次结算写 `ingest.log`。

### 8.3 轨迹与统计

- 所有 GPS 点入库（移动 2s / 静止停发），按时间分段累计；
- **每日统计**：网页轨迹页按天查询（行程数/里程/时长/最高速度/点数），可切"全部"看历史累计；
- **行程拆分**：相邻轨迹点间隔 >5 分钟视为新行程；行驶时长只累计移动段（相邻点位移 >20m）。

## 九、网页前端设计

单文件 `index.php`（深色风格）：

### 页面布局

- **顶部 Tab**：📡 GPS / 🔋 BMS / 🚗 轨迹 / ⚙️ 设置 / 退出；
- **GPS 页**：高德地图实时定位 + 轨迹线（按速度着色）+ 信息卡（状态/模式/速度/经纬度/续航/今日轨迹/4G 信号/蓝牙/天气/更新时间）；
- **BMS 页**：电池状态（总电压/电流/功率/SOC/SOH/最高最低单体/压差/温度/循环次数/运行时间/MOS 状态/容量/续航估算）+ 单体电压网格 + 线束内阻；蓝牙未连接时显示「⚠ BMS 未连接」，不显示残留数据；
- **轨迹页**：今日行驶 + 每日统计（按天查询，含"全部"）+ 今日行程 + 按日期轨迹回放（地图动画播放、倍速 1x~8x）；
- **设置页**：刷新频率（网页轮询/BMS/GPS 间隔/保留天数）、高德地图（Key/安全密钥）、登录账号（改密码）、设备远程控制（远程重启）、流量参考。

### 前端逻辑

- `setInterval` 按「网页刷新间隔」轮询 `/api/latest`（默认 3s）；所有请求 `cache:'no-store'`；
- 每 5s `/api/heartbeat` 标记在线；页面关闭后服务端 25s 超时自动下发 track；
- 天气：高德 `AMap.Weather` + 逆地理编码，10 分钟刷新；
- **数据过期检测**：`lastUpdate` 超过 90 秒判定离线（>30s 上报间隔，停放时 GPS/BMS 停发但 blestatus 30s 保活，不会误报）；
- **今日轨迹**：按当天本地 0 点起算；Tab 切换用 `visibility` 保证地图容器正常渲染。

### 依赖

- 高德地图 JS API v2.0（Web 端 Key + 安全密钥，设置页在线配置）；无其他前端框架。

## 十、部署

完整步骤与自检清单见 **[`部署清单.md`](部署清单.md)**，要点：

1. 上传 `01-网页服务端/` 全部文件 + `.htaccess` + 空 `data/` 到虚拟主机根目录（PHP ≥ 7.4，需 pdo_sqlite/curl/openssl）；
2. 改 `config.php`：MQTT 直发凭据（客户端认证账号）/ WebHook token / 登录密码；
3. EMQX 控制台：**客户端认证**（下行直发用账号）+ **转发规则**（`SELECT payload FROM "T"` → HTTP 服务 → `api.php?action=ingest`，X-Ingest-Token 头）；
4. 烧录固件 v2.15：首次用 `02-ESP32固件/firmware/esp32s3-gateway-v2.15-4MB-OTA.merged.bin`（仓库已发布）全量刷（4MB 分区表）；之后可经 ESP32 Web 后台「固件升级」OTA（上传 `esp32s3-gateway-v2.15-OTA.bin`）。DTU 配 EMQX 8883 + T/R 主题，固件 Web 后台填 BMS MAC；
5. 可选 cron：每分钟 `api.php?action=checkmode&token=<token>`（兜底离线收敛；服务端已内置 30s 周期强制下行，网页关闭也能自愈）。

## 十一、风险与注意事项

1. **EMQX Serverless 仅 TLS（8883）**：DTU 用「无证书加密」模式直连（实机验证可行）；
2. **M100PG-DTU GPS 数据**：`gpsext` 不含卫星数（数据源限制，网页只显示已定位/未定位）；GPS 需室外开阔环境；
3. **服务端稳定性**：依赖 EMQX 规则转发与虚拟主机可用性；转发失败自动重试；可选 cron 兜底；设备侧三层自愈兜底；
4. **流量控制**：网页开 2h/天 ≈ 35MB/月（GPS 移动 2s + BMS 2s），300MB 套餐余量充足；网页关闭后大幅下降（GPS 静止停发、BMS 30s ≈ 0.36MB/天）；
5. **BLE 断线重连**：固件断线自动重连（沿用参考代码扫描重连逻辑）；
6. **时间同步**：数据入库统一用服务器时间（强制北京时间），避免设备无 RTC 时间漂移；
7. **地图偏移**：GPS 为 WGS84，服务端转 GCJ02（range.php `wgs84ToGcj02`）对齐高德；
8. **WiFi AP 与 BLE 共存**：Web 后台（AP）+ BLE 中继同时运行存在射频干扰，固件沿用低发射功率 + 大 beacon 间隔 + 10 分钟无操作自动关 WiFi 策略。

## 附：参考来源

| 参考代码 | 本方案复用内容 |
|---|---|
| 参考代码1 `bms-parser.js` | BMS 帧解析逻辑 → 移植为 PHP `parser.php` |
| 参考代码1 `server.js` | 里程学习、haversine → 移植为 PHP `range.php` |
| 参考代码2 `极空保护板蓝牙4G.html` | 页面样式、BMS 展示、高德地图轨迹 → 精简为 `index.php` 看板 |
| `中继完整版.txt`（v9.3） | 固件 BLE 基础：NimBLE 按 handle 区分双 FFE1、帧头拼帧、Web 后台、多客户端中继（帧级转发 CRC + 双通道透传 + 特征选择兼容 V17~V20 + 广播用中继名）；轮询策略：无订阅 5s / 有订阅 30s |
| 参考代码2 `esp32s3.ino` | 固件 4G/GPS 部分：串口指令集（chaxun/getcsq/getgps/getblestatus）+ GPS 指令查询（config,get,gpsext）+ 顺序查询节奏 |
